spring.application.name=Backend_J2EE
spring.config.import=optional:file:.env[.properties]
# MySQL Database Configuration
<<<<<<< HEAD
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQL_USERNAME}
spring.datasource.password=${MYSQL_PASSWORD}
=======
spring.datasource.url=jdbc:mysql://localhost:3306/j2ee_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root

spring.datasource.password=



>>>>>>> 02a191ea3fe916f5e02244a6219fe6f6ae8c50a7
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
# Load seed data on startup. INSERT IGNORE keeps the script idempotent.
spring.sql.init.mode=always
spring.sql.init.continue-on-error=false

# File upload
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
app.upload.dir=uploads

# Google OAuth (set from environment variable)
app.google.client-id=${GOOGLE_CLIENT_ID}

# Chat realtime
app.chat.timeout-seconds=180
app.chat.super-admin-usernames=

# RAG chatbot + vector DB
app.rag.retrieval.top-k=${APP_RAG_RETRIEVAL_TOP_K}
app.rag.provider=${APP_RAG_PROVIDER}
app.rag.embedding.model=${APP_RAG_EMBEDDING_MODEL}
app.rag.embedding.fallback-dimensions=${APP_RAG_EMBEDDING_FALLBACK_DIMENSIONS}
app.rag.chat.model=${APP_RAG_CHAT_MODEL}
app.rag.base-url=${APP_RAG_BASE_URL}
app.rag.api-key=${APP_RAG_API_KEY}

# VNPay sandbox config (lay tu vnpay_nodejs/config/default.json)
vnpay.tmn-code=AFHY5UKO
vnpay.hash-secret=A67W4EVFQOSKGMO5U38Y5HT20WFI0LE2
vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.api-url=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
vnpay.return-url=http://localhost:8080/api/vnpay/vnpay-return
